# https://ieeexplore.ieee.org/document/11107436/footnotes#footnotes

## 提及“高子域熵、异常大的有效载荷大小、罕见的查询频率”是隐蔽信道的最特别特征。这意味着 子域、载荷 是值得累积的

## 提及“NULL”查询几乎从未被合法利用，可以全部屏蔽（蛤？）

## 提及了十一个公用的特征：
  
  packet_interarrival：连续DNS数据包之间的平均时间，捕捉流量突发性（由于数据窃取，隧道通常表现出更短的间隔）。
  
  retry_count：查询重试次数，指示持久性（由于不可靠的信道，隧道中更高）。
  
  avg_retry_time：重试之间的平均时间，反映重试频率。
  
  var_retry_time：重试时间的方差，衡量重试一致性。
  
  query_entropy：查询名称的香农熵，捕捉随机性（隧道使用编码子域，例如base64，产生更高的熵，均值4.95对比4.38）。
  
  ttl_variance：TTL值的方差，作为一个关键判别器（隧道显示接近零的方差，均值0.0002，对比正常流量中的2.5×10^8）。
  
  avg_response_size：DNS响应有效载荷的平均大小（隧道响应通常因编码数据而更大）。
  
  query_response_ratio：查询与响应的比率，指示交互模式。
  
  domain_switching：查询中域名更改的频率，反映隧道对多个子域的使用。
  
  query_type_changes：不同DNS记录类型（例如A， TXT）的数量，由于协议多样性，在隧道中更高。
  
  query_count：查询总数，捕捉流量体积。

在证据累积方面，对应需要累积的证据有：时间，TTL，有效载荷的大小，域名，记录类型。具体的特征提取与判断交给下一个部分。

## 该文献算法中，使用了以下过滤：

  为了确保提取有用且一致的特征，应用了严格的过滤过程来净化每个PCAP文件的原始DNS流量。这对于消除噪声、错误数据包和可能危及特征集完整性的不需要流量是必要的。由于数据集主要包含DNS-over-UDP，即使特征集设计为泛化到加密DNS协议如DoH，并计划未来扩展数据集，我们仅使用常规的DNS-over-UDP流量进行分析。应用了以下限制：
  
  协议验证：每个数据包必须包含IP、UDP和DNS层，排除加密变体如DNS over HTTPS或DNS over TLS。
  
  DNS语义检查：解析DNS头部，基于查询/响应（QR）标志验证数据包是查询还是响应。这两种类型被分开处理，因为它们贡献不同的特征。
  
  域名可提取性：数据包必须在DNS问题部分包含一个有效的qname字段。如果缺失、格式错误或包含无法解码的字节（例如非UTF-8），则排除该数据包。
  
  单一问题假设：仅包含具有单个DNS问题（qdcount = 1）的数据包，排除罕见的多问题数据包以保持特征一致性。
  
  错误处理：引发解析错误（索引异常、缺失字段、解码错误）的数据包被静默跳过，以防止会话级损坏。
  
  这种过滤策略确保特征计算仅基于结构有效且语义上有意义的DNS交换，支持泛化并最小化偏倚数据。该过程在算法1中详述。

## 有关于过滤的伪代码（哇哦~）

1:  对于 PCAP 中的每个数据包 p 执行

2:      如果 p 包含 IP、UDP 和 DNS 层 则

3:          提取 DNS 层 dns

4:          如果 dns 具有有效的 QR 标志 且 dns.qdcount = 1 则

5:              如果 qname 存在 且 可解码 则

6:                  将 p 添加到 filtered_packets

7:              结束如果

8:          结束如果

9:      结束如果

10: 结束对于

11: 返回：filtered_packets

# https://jcs.iie.ac.cn/xxaqxb/ch/reader/view_abstract.aspx?flag=2&file_no=202212280000001&journal_id=xxaqxb

## 14个具体特征：

<img width="918" height="629" alt="image" src="https://github.com/user-attachments/assets/26b604cd-2d01-48ad-87ad-d2dcdb0afba2" /># https://ieeexplore.ieee.org/document/11107436/footnotes#footnotes
-某一域名下的DNS流量
-同域名的主机名个数
-域名结构性特征
-会话时长占比
-请求包及响应包大小
-请求应答比
-请求响应时间间隔
-孤立的请求个数DNS 流量/IP地址请求端IP的高散性响应端IP的高散性
-NXDomain 响应数量
-DNS服务器地理位置
-DNS流量态势异常
